import "./Navbar.css";

const Navbar = () => {
  return (
    <div>
      <nav>
        <a href="#">Home</a>
        <a href="#">Add Recipe</a>
      </nav>
    </div>
  );
};

export default Navbar;
